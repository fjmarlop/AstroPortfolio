---
interface Props {
	title: string;
	href: string;
}

const { href, title } = Astro.props;
---

<button
    class=" hover:bg-teal-600 text-white font-bold py-2 px-4 rounded-full md:text-2xl"
>
   <a href={href}>{title}</a>
</button>